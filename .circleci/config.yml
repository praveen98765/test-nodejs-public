version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
  notify:
    docker:
      - image: circleci/node:10
    steps:
      - run: echo "hello"

workflows:
  version: 2.1
  build_notify:
    jobs:
      - build
      - notify:
          requires:
            - build
      